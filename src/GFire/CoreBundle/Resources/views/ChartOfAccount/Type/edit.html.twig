{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ChartOfAccount\Type edit</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('chartofaccount_type') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
